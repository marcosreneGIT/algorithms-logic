// Soma

#include <stdio.h>

int main(void)
{
    int x, y, soma;

    printf("\nInforme o valor de X: ");
    scanf("%d", &x);

    printf("Informe o valor de Y: ");
    scanf("%d", &y);

    soma = x + y;

    printf("\nA soma total e de: %d", soma);

    return (0);
}